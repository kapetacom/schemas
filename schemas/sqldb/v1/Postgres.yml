kind: core.blockware.com/v1/ResourceType/Operator
metadata:
  id: sqldb.blockware.com/v1/PostgreSQL
  name: Postgres

schema:
  type: object
  properties:
    metadata:
      type: object
      properties:
        name:
          type: string
      required:
      - name
  required:
  - metadata

spec:


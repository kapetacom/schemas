kind: core/block-type
metadata:
  name: blockware/block-type-frontend
  title: Frontend
spec:
  schema:
    type: object
    properties:
      target:
        $ref: core/language-target-reference
      entities:
        $ref: core/entity-list
      consumers:
        $ref: core/block-resource-list
      providers:
        $ref: core/block-resource-list
    required:
      - target
  dependencies:
    - consumers.kind
    - providers.kind
  entities:
    - entities.types


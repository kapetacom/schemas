hasAccess:
  responseType:
    type: boolean
  method: GET
  path: /oauth2/clients/{clientId}/access
  arguments:
    clientId:
      type: string
      transport: PATH
      optional: true
    request:
      ref: ClientAccessRequest
      transport: QUERY
      optional: true

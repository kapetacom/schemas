kind: core.blockware.com/v1/ResourceType/Operator
metadata:
  id: sqldb.blockware.com/v1/MySQL
  name: MySQL

schema:
  type: object
  properties:
    metadata:
      type: object
      properties:
        name:
          type: string
      required:
      - name
  required:
  - metadata

spec:


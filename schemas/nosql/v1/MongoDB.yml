kind: core.blockware.com/v1/ResourceType/Operator
metadata:
  id: nosqldb.blockware.com/v1/MongoDB
  name: MongoDB

schema:
  type: object
  properties:
    metadata:
      type: object
      properties:
        name:
          type: string
      required:
      - name
  required:
  - metadata

spec:


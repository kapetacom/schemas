kind: core/block-type
metadata:
  name: some-handle/my-type
  title: My Block Type
  description: This adds a new block type to your system
spec:
  schema:
    type: object
    properties:
      dockerImage:
        type: string
      dockerTag:
        type: string
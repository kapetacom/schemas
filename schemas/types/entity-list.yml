$id: /core/entity-list
$schema: http://json-schema.org/draft-07/schema#
title: EntityList
definitions:
  SourceCode:
    type: object
    properties:
      type:
        type: string
      value:
        type: string

type: object
properties:
  types:
    type: array
    items:
      $ref: /core/entity
  source:
    $ref: "#/definitions/SourceCode"

